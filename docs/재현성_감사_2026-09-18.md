# 재현성 감사 — 2026-09-18

작성 2026-09-18 19:55~22:30 KST · 수행 로컬 Claude Code · 작업 위치 `C:\Users\대전\dflow-alley-width`(main) ·
지시서 `작업지시서_저장소_재현가능화.md`(P0~P3) + `작업지시서2_전체실행_2026-09-18.md`(21:15 추가분 중 겹치는 항목)

**실행 환경** — Windows 11 Home · 16 GB(실행 시 여유 2.2~6.8 GB) · Python 3.13.12 ·
비공개 작업 저장소의 venv(`data/vworld/.venv`: torch 2.14.0+cpu · segment_anything · pyogrio 0.13.0 · geopandas 1.1.4 · numpy 2.4.x)를 **읽기 전용으로 빌려** 실행.
표준노드링크 `NODELINKDATA_20260914/`(801 MB)와 SAM 가중치 `models/`(358 MB)는 비공개 저장소 폴더를 **디렉터리 정션**으로 연결(쓰기 없음), 정사영상 타일 캐시(422장 · 7.7 MB)는 복사.
브이월드 키는 사용자 환경변수 `VWORLD_APIKEY`(앞에 공백 1자가 섞여 있어 37자 → `.strip()` 36자) · `VWORLD_DOMAIN=localhost:8765`.
실행 전 `deliverables/`·`derived/` 전체를 `_audit/before/` 로 복사하고, 실행 후 `diff --strip-trailing-cr` 로 파일마다 비교했다(`_audit/cmp.sh`). `_audit/` 는 `.gitignore`.
비공개 저장소는 수정하지 않았고, 제출본 폴더는 열지 않았고, 푸시하지 않았다.

---

## 1. 이관 목록 (비공개 `docs/vworld/` → 공개)

### 복사한 것
- **스크립트 41개** — `access37_geocode · ai_a_classifier · alley_missing_census · anchor_figure · build_hwpx · daejeon_cctv_check · daejeon_cctv_match · e5_export_layers · e5_server · factsheet · figure_047_national · figures_027 · figures_final_004 · fire_water_coverage · height_jurisdiction_check · hwpx_pdf_pages · lanes_direction_check · layout_variance · missing_alley_crosscheck · missing_rates_bridge_tunnel · model_availability · ngii_bound_cover · ngii_five_way · ngii_medial_width · ngii_obstacle_diag · ngii_width_probe · nodelink_scope_stats · ortho_crosscheck · ortho_resolution · road_bt_crosstab_export · road_bt_daejeon · sam_validation · save_as_hwp · sensitivity_boundary · site_table_26 · slope_26 · slot_gantt_figure · slot_itaewon_demo · slot_itaewon_sop3 · vworld_layer_catalog · water_slot_buildings`
- **산출물 12개** — `26개소_산출표.md · 3단표.md · AI-A_모델유사골목_상위.csv · AI-B_SAM검증_4자대조.csv · AI-C_도로경계내접폭_5자대조.csv · AI보완_커버리지표.csv · ITS영상CCTV_26개소.csv · 대전_단속CCTV_골목급대조.csv · 이름은있는데쓸수없는속성.md · 조판편차_실측.json · 주장_출처대조_사람판정.csv · 주정차허용_26개소.csv`
- **조사 문서 3종** → `docs/vworld/` — `데이터수급_실사.md · 학술문헌_조사.md · 기술조사_임무배정.md`(바이트 그대로 · `audit_claims.py` 근거 파일)
- **사전 확정 본문 10개** → `docs/prereg/<단축해시>_판정기준.md` — 비공개 저장소 git 에서 `git show <커밋>:docs/vworld/회전기하_판정기준.md` 로 꺼냄(읽기). `git hash-object` 가 `PREREG.md` 표의 blob SHA-1 과 **10/10 일치**. `.gitattributes` 로 줄끝 변환 차단.
- **작은 원천 5종**(공공데이터포털 표준데이터 · 재배포 가능) → `data/vworld/national/raw/` — `대전광역시_소방차 진입곤란지역 현황.csv`(3.9 KB) · `daejeon_access_geo/소방차_진입곤란지역_현황.{shp,dbf,shx,prj,cpg}`(113 KB) · `daejeon_cctv/대전광역시_불법주정차 단속구간(고정형 CCTV 단속구간).csv`(59 KB) · `firewater/std_firewater_20240213.csv.gz`(7.3 MB · 평문 52 MB 는 `.gitignore`) · `국토교통부_전국교량표준데이터_20251231.csv`(11.6 MB) · `국토교통부_전국도로터널정보표준데이터_20251231.csv`(1.2 MB)

### 복사하지 않은 것과 이유
| 파일 | 이유 |
|---|---|
| `scripts/fill_official_form.py` | 제출자 성명·이메일(`korea.kr`)·소속이 코드 안에 상수로 있음 |
| `scripts/form_identity.py` · `scripts/preflight.py` | 제출자 성명이 문자열로 있음(양식 대조 · 제출본 파일명 정규식). 제출본 `.hwpx` 전용 도구 |
| `deliverables/검토_팩트시트.md` · `프리플라이트_결과.md` | 제출본 파일명(성명 포함)이 본문에 있음 |
| `deliverables/저장소_민감어스캔.md` | 성명 2건이 본문에 있음 · 비공개 저장소 파일 경로 목록 |
| `scripts/superseded/typeset_hwp_com.py` | 하위 폴더(지시 범위 밖) · 한글 COM 조판 도구 |
| `raw/NODELINKDATA_20260914/`(801 MB) · `raw/[2026-09-14]NODELINKDATA.zip`(271 MB) · `models/sam_vit_b_01ec64.pth`(358 MB) · `ortho_cache/`(이용조건) · `raw/firewater/*.csv` 평문(52 MB) | 용량·이용조건 — `.gitignore`. 받는 방법은 README ③ |
| 비공개 `data/vworld/e5/` · `docs/vworld/e5/` · `docs/vworld/figures/` · `DFLOW_008R9R4_CORE/` | 지시 범위 밖. `e5_*.py` · `figures_*.py` · `slot_itaewon_{demo,sop3,arm3_width}.py` 는 이것이 없어 공개 저장소에서 돌지 않는다(§5). `slot_itaewon_width.py` 는 코어 모듈을 쓰지 않아 공개 저장소만으로 돈다 |

키 패턴(`key=…20자+` · `apikey` · `confmkey` · UUID) 검출 0. 개인정보 패턴(전화·주민번호·이메일·성명) 검출은 README 저자 표기와 `vworld@spacen.or.kr`(기관 공개 메일)뿐.

### 경로 수정(로직 불변)
복사한 41개: `parents[3]`→`parents[1]` · `docs/vworld/deliverables`→`deliverables` · `docs/vworld/scripts`→`scripts` · `docs/vworld/회전기하_판정기준.md`→`docs/판정기준.md` · `docs/vworld/figures`→`figures` · `CONTEST-VWORLD-`→`D-`.
복사한 산출물 중 `26개소_산출표.md` · `주장_출처대조_사람판정.csv` 의 경로 문자열도 같은 규칙으로 바꿨다(값 아님). 조사 문서 3종은 바꾸지 않았다.
추가로 발견해 고친 것:
- **브이월드 요청 전부** — 키 `.strip()` + `domain=`(`VWORLD_DOMAIN`, 기본 `localhost`) 추가(23개 스크립트). 이것 없이는 모든 WFS/WMS 가 `ServiceExceptionReport`·`INCORRECT_KEY` 로 실패한다(1차 `linkage_table.py` 50행 전부 U3 조회 실패로 실측).
- `road_bt_crosstab_export.py` · `ai_a_classifier.py` · `daejeon_cctv_match.py` — 평문 `.jsonl` 직접 open → `_rawio.open_raw`(.gz 대체). `water_slot_buildings.py` — `raw_path`(.csv.gz 대체).
- `.gitignore` — **줄 끝 주석이 패턴의 일부로 읽혀 `NODELINKDATA_*/`·`models/`·`ortho_cache/`·`*.zip`·`*.jsonl` 이 하나도 무시되지 않고 있었다**(`git check-ignore` 로 확인). 주석을 별도 줄로 옮겨 고쳤고, 지금은 정션·가중치·타일·zip 이 무시되고 `derived/*`·`raw/*.jsonl.gz`·`원천/*`·`sam_vehicle_cache/` 는 무시되지 않는다.

---

## 2. 실행 표

종료코드 0 = 정상. 「동일」 = 실행 전 사본과 CR 무시 바이트 일치. 소요는 이 PC 실측.

### 2-1. 핵심 사슬 — SAM (지시서 2 A·B)
| 스크립트 | 명령 | 종료 | 소요 | 커밋본과 동일? | 비고 |
|---|---|---|---|---|---|
| sam_vehicle_width.py | `python scripts/sam_vehicle_width.py` · **마스크 캐시 48개를 치우고** 처음부터 | 0 | **100 분**(20:07→21:47 · 조각당 마스크 ~130 s + 검증창 ~120 s) | **동일** — `sam_vehicle_width.json` · `통행가능폭_주차차감_37조각.csv` · `회랑필터_전후_오탐.csv` · `차량탐지_사람대조_11조각.csv` 4개 모두 | 원 마스크 3,838 · 규격 통과 709 · 회랑 안 351 · 차량 후보 71 · 제외 638 · 90.0 % · 사람대조 9/5. **새로 만든 마스크 캐시 48개 파일(work 37 + val 11)이 원 캐시와 바이트 단위로 전부 같다.** 타일은 캐시 422장으로 충당(키 호출 0). `figures/도표9*.png` 3장 생성 |
| verify_deliverables.py | 재실행 뒤 | 0 | 0.5 | — | 통과 38 · 실패 0 유지 |
| sensitivity.py | 새 캐시로 | 0 | 4.3 | 동일(CSV 2 + json) | 86.5~100 % · 89.2 % |
| sensitivity_boundary.py | | 0 | 2.7 | 동일 | 10건 |
| determinism_check.py | | 0 | 156.0 | **소요초 127→140 한 줄만 다름** | 변정7길 마스크 93 · spec 15 · corr 21 · conf 5 · lat_max 2.21 · 행 단위 완전 일치 true · torch 2.14.0+cpu |
| model_availability.py | | 0 | 49.9 | (§2-4 표) | |

### 2-2. 무의존·캐시 계열
| 스크립트 | 명령 | 종료 | 소요 | 동일? | 비고 |
|---|---|---|---|---|---|
| verify_deliverables.py | 인자 없음 | 0 | 0.5 | — | 통과 38 · 실패 0 |
| verify_numbers.py | `docs/서식2_본문_2026-09-18.txt .` | 0 | 0.6 | — | PASS. **인자 없이는 IndexError** → README 수정 |
| slot_time_stage1.py | | 0 | 0.5 | 동일 | 37행 |
| transit_inflow.py · transit_inflow_volume.py | | 0 · 0 | 4.1 · 0.7 | 동일(3파일) | |
| road_bt_national.py | `agg` | 0 | 8.3 | 동일 | 875,892구간 · 잎칸 3,474 |
| road_bt_crosstab_export.py | | 0 | 12.8 | 동일(A·B) | 1차는 평문 .jsonl 없어 실패 → `_rawio` |
| site_table_26.py | | 0 | 0.6 | 동일(csv·md·json) | |
| water_slot_buildings.py | | 0 | 22.7 | 동일(3 CSV) · json 3개 rule 1줄 | `.csv.gz` 로도 동일 확인 |
| ai_a_classifier.py | `run` | 0 | 158.7 | 동일 · json rule_doc 1줄 | |
| daejeon_cctv_match.py | | 0 | 5.9 | 동일(csv·json) | 1차는 raw/daejeon_cctv 없어 실패 → 원천 CSV 추가 |
| ngii_five_way.py | | 0 | 0.5 | 동일(csv·json) | |
| audit_claims.py | `서식2_본문.txt . 주장_출처대조_사람판정.csv` | 0 | 11.8 | **216행 vs 커밋 190행 — 다름** | §3 |
| missing_rates_bridge_tunnel.py | | 0 | 2.7 | (§2-4) | 교량·터널 CSV 추가 |
| fire_water_coverage.py | | 0 | 3.6 | (§2-4) | 평문 CSV 필요(sha256 계산) — 실행 후 평문은 무시 목록 |
| 새 venv(`requirements.txt` 핀 그대로 · torch 제외 설치 · torch `--dry-run`) | site_table_26 · road_bt_crosstab_export · transit_inflow_volume | 0 | | 동일 | Python 3.13 · numpy 2.5.3 · pandas 3.0.5 · shapely 2.1.2 등 설치 성공 |

### 2-3. 브이월드 키 계열
| 스크립트 | 명령 | 종료 | 소요 | 동일? | 비고 |
|---|---|---|---|---|---|
| vworld_api_probe.py | | 0 | 3.5 | **다름 — 커밋본 유지** | `domain` 없는 변형(a_none)이 지금은 `ServiceExceptionReport`·`INCORRECT_KEY`. 09-15 에는 성공했음. API 정책 변화의 시점 기록이므로 커밋본(09-15)을 둔다 |
| vworld_layer_probe_26.py | | 0 | 124.2 | 동일 | 592건 조회 실패 0 |
| linkage_table.py | | 0 | 34.3 | 조회일(09-16→09-18)·rule_doc 외 **동일** — 교체 | 1차(domain 없음) 469 s · 50행 전부 U3 |
| inclusion_rate.py | | 0 | 26.9 | CSV 동일 · json rule_doc 1줄 | 어제 프록시 403 → 오늘 성공 |
| ngii_medial_width.py | | 0 | 20.6 | CSV 동일 · json rule_doc 1줄 | |
| slope_26.py | | 0 | 31.6 | CSV 동일 · json rule_doc 1줄 | |
| missing_alley_crosscheck.py | | 0 | 17.4 | CSV 동일 · json rule_doc 1줄 | |
| access37_geocode.py | `--v2` | 0 | 6.7 | json rule_doc 1줄 | |
| ngii_width_probe.py · ngii_bound_cover.py · ngii_obstacle_diag.py · height_jurisdiction_check.py · ortho_resolution.py · road_bt_daejeon.py · access37_geocode.py(v1) | | (§2-4) | | | |

### 2-4. 파생 JSON 만 만드는 보조 스크립트(지시서 2 C·D 추가분)
| 스크립트 | 산출 | 종료 | 소요 | 동일? | 비고 |
|---|---|---|---|---|---|
| missing_rates_bridge_tunnel.py | derived/missing_bridge_tunnel.json | 0 | 2.7 | 동일 | 교량·터널 표준데이터 CSV 를 raw/ 에 추가(12.8 MB) |
| fire_water_coverage.py | derived/fire_water_coverage.json | 0 | 3.6 | 동일 | 평문 CSV 의 sha256 을 계산하므로 평문이 필요(`.gitignore`) |
| model_availability.py | derived/model_availability.json | 0 | 49.9 | **다름 — 유지** | 표본 타일이 달라 마스크 64→48. 「SAM 이 이 PC 에서 도는가」 기록이라 커밋본 유지 |
| ngii_width_probe.py | derived/ngii_width_probe.json | 0 | 1.5 | 동일 | |
| ngii_bound_cover.py | derived/ngii_bound_cover.json | 0 | 131.9 | **다름 — 유지** | 재실행에서 10조각이 WFS **미조회**(`"error": "미조회"` 로 기록됨 · 0건으로 쓰지 않음). 커밋본은 37조각 전부 조회 성공 |
| ngii_obstacle_diag.py | derived/ngii_obstacle_diag.json | 0 | 31.9 | 동일 | |
| height_jurisdiction_check.py | derived/height_jurisdiction_check.json | 0 | 13.2 | 동일 | |
| lanes_direction_check.py | derived/lanes_direction_check.json | 0 | 47.2 | 동일 | MOCT_LINK |
| ortho_resolution.py | derived/ortho_resolution.json | 0 | 124.3 | 수치 동일 · 타일 출처 플래그 `fetched`→`cache` 만 다름 — 유지 | |
| road_bt_daejeon.py | derived/road_bt_daejeon.json | 0 | 81.7 | 동일 | 대전 WFS 323칸 재조회 · 9,294구간 — **브이월드 현행 데이터가 09-15 와 같음을 대전 범위에서 확인** |
| alley_missing_census.py | derived/alley_census.json · figures/도표A·B | 0 | 56.0 | 동일 | MOCT_LINK |
| access37_geocode.py | (v1) derived/access37_geocode.json | 0 | 5.5 | **다름 — 유지** | 지오코더 응답에 필드가 추가되는 등 176줄 차이(v2 는 rule_doc 1줄뿐). 재실행본 `_audit/rerun_access37_geocode_v1_2026-09-18.json` |
| nodelink_scope_stats.py | — | 미실행 | | | 비공개 `docs/busan/raw/*링크정보*` 필요 |

### 2-5. 표준노드링크 계열
| 스크립트 | 명령 | 종료 | 소요 | 동일? | 비고 |
|---|---|---|---|---|---|
| national_inclusion.py | `agg` | 0 | 46.0 | CSV 동일 · json rule_doc 1줄 | 152,944 · 33,336 · 78.2 % |
| hierarchy_compare_v2.py | `agg` | 0 | 42.3 | 동일 | |
| hierarchy_compare.py | | 0 | 153.4 | **다름 — 커밋본 유지** | 전국 2행(수치)은 동일. 재실행은 `lt_c_adsido` 결합이 지금은 100폴리곤을 받아 **영문 시도명 행 48줄이 추가**되고 미결합 링크 1,132,610(72 %)이 그대로 — 커밋본 §16 주석이 말하는 바로 그 결함이라 시도별은 `_길이.csv` 가 기준. 재실행본은 `_audit/rerun_hierarchy_compare_2026-09-18.json` |
| turn_geometry_census.py | | 0 | **109.0** | **동일**(`turn_census.json` 및 CSV 4종) | 어제 7 GB 컨테이너에서 OOM → 오늘 16 GB PC · 여유 6.7 GB 에서 **코드 수정 없이 완주**. 지시서 2 D-1 의 청크 패치는 필요 없었으므로 적용하지 않았다 |
| lanes_direction_check.py · alley_missing_census.py · nodelink_scope_stats.py | | (§2-4) | | | |

### 2-6. 돌리지 않은 것(이유)
| 스크립트 | 이유 |
|---|---|
| acquire_open.py | 무인자 실행은 6개 원본 전부 다운로드. `nodelink` 는 271 MB 를 받아 `NODELINKDATA_20260914/` 에 풀며 **그 경로가 지금 비공개 저장소로의 정션**이라 비공개 저장소에 쓰게 된다 → 실행 안 함 |
| road_bt_national.py `fetch` · hierarchy_compare_v2.py `fetch`/`sido` · national_inclusion.py `sigg` · ai_a_classifier.py `fetch` · daejeon_cctv_check.py · vworld_layer_catalog.py | 원시 캐시(`raw/*.jsonl` · `raw/daejeon_cctv/`)를 **새로 받아 덮어쓰는 수급 단계**. 브이월드 데이터가 바뀌었으면 제출본이 인용한 전국 수치가 흔들리므로 지시서 2 의 규약대로 돌리지 않았다 |
| sam_validation.py | torch + 브이월드 WFS. 시간 부족(SAM 사슬 100 분 뒤) — **미실행**. 산출 `AI-B_SAM검증_4자대조.csv` · `AI보완_커버리지표.csv` 는 원 실행본 그대로 |
| ortho_crosscheck.py · anchor_figure.py · figures_027.py · figure_047_national.py · figures_final_004.py · slot_gantt_figure.py | 도표 PNG 생성 도구. 재현 대상 수치를 만들지 않음. 일부는 `Downloads/브이월드_공모전/` 사본을 쓰므로 접근 금지 폴더 |
| e5_server.py · e5_export_layers.py | 비공개 `data/vworld/e5/` · `docs/vworld/e5/` 필요(공개 저장소에 없음) |
| slot_itaewon_demo.py · slot_itaewon_sop3.py | 비공개 `DFLOW_008R9R4_CORE/` 모듈 필요 |
| build_hwpx.py · hwpx_pdf_pages.py · layout_variance.py · save_as_hwp.py · factsheet.py | 제출본 `.hwpx` · 한글 COM 필요. `factsheet.py` 는 `--pdf-json` 인자 필수이며 성명이 든 `검토_팩트시트.md` 를 만든다 |

---

## 3. 커밋본을 교체한 산출물 / 유지한 산출물

### 교체(재실행본으로) — 수치 동일, 문자열만 낡았던 것
- `derived/*.json` 13개의 `rule_doc`/`rule` 1줄(`docs/vworld/회전기하_판정기준.md` → `docs/판정기준.md`): access37_geocode_v2 · adjacent_buildings_26 · ai_a_classifier · inclusion_rate · missing_alley_crosscheck · national_inclusion · ngii_medial_width · slope_26 · slot_order_26 · water_reach_26 · linkage_table(+`date` 09-16→09-18) · determinism_check(`소요초` 127→140). 문서를 옮긴 뒤 산출물을 다시 뽑지 않아 생긴 차이이며, 수치는 전부 같다.
- `derived/linkage_table.csv` — `조회일` 열만 09-16→09-18.
- 되돌리려면 `git checkout df22249 -- <경로>` 한 줄이다.

### 유지(커밋본 그대로) — 재실행이 달랐던 것
| 파일 | 차이 | 원인 | 유지 이유 |
|---|---|---|---|
| `deliverables/주장_출처대조.csv` | 커밋 190행 ↔ 재실행 216행(저장소 산출 154→174 · 1차 원문 29 · 2차 인용 7→13 · 미확인 0→0) | **입력이 다르다** — 커밋본은 제출본 `.hwpx`(저장소에 없음, 문단 503), 재실행은 `docs/서식2_본문_2026-09-18.txt`(.hwp 본문 추출, 문단 538). 커밋본은 근거로 `프리플라이트_결과.md`(저장소에 없음)도 가리킨다 | 지시서 2 「덮어쓸지는 사람이 판단」. 재실행본은 `deliverables/주장_출처대조_txt재실행_2026-09-18.csv` 로 **병기**. 어느 쪽을 정본으로 할지 결정 필요 — 공개 저장소만으로 재현되는 것은 216행판 |
| `derived/vworld_probe.json` | 09-15 실행은 `domain` 없는 요청도 성공, 오늘은 실패 | 브이월드 API 정책 변화(시점 기록) | 시점 기록이라 유지 |
| `derived/hierarchy_compare.json` · `deliverables/도로명위계_전국대조.csv` | 재실행에 영문 시도 행 48줄 추가 | `lt_c_adsido` 결합이 오늘은 폴리곤 100건을 받음(그래도 72 % 미결합) | 전국 2행은 동일 · 시도별은 §16 주석대로 `_길이.csv` 가 기준 · 커밋본의 수기 주석 행 보존 |

### 이상 징후(원인 불명 · 기록)
`derived/sensitivity.json` 이 20:47:44 에 부분 마스크 캐시 수치(규격통과 243 · 후보 24 · 불일치 16)로 바뀌어 있었다. 이 감사의 실행 기록에 그 시각의 `sensitivity.py` 실행은 없고, 같은 스크립트가 함께 쓰는 CSV 2종은 바뀌지 않았다(mtime 19:33). GitHub Desktop 이 20:46:03 에 `index.lock` 을 남긴 것과 시각이 가깝다. 커밋본으로 복원한 뒤 SAM 완료 후 재실행해 **커밋본과 동일**함을 확인했다. 부분 수치 파일은 `_audit/sensitivity_json_unexpected_20-47-44.json` 에 보관.

---

## 4. 여전히 재현되지 않는 것 (정확히 무엇이 없어서)

| 무엇 | 왜 | 없는 것(파일·용량) |
|---|---|---|
| `주장_출처대조.csv` 190행판 | 입력 `.hwpx` 가 저장소에 없음(서식1 개인정보) | 제출본 `.hwpx` · `프리플라이트_결과.md` |
| `조판편차_실측.json` | 제출본 + 한글 COM | 위와 같음 |
| `AI-B_SAM검증_4자대조.csv` · `AI보완_커버리지표.csv` | 오늘 `sam_validation.py` 를 돌리지 못함(시간) — 환경은 갖춰져 있음 | — (미실행) |
| `derived/model_availability.json` · `ngii_bound_cover.json` · `access37_geocode.json`(v1) · `vworld_probe.json` | 재실행이 다름(표본 타일 · WFS 미조회 · 지오코더 응답 변화 · API 정책) — 시점 의존 기록 | 같은 시점의 브이월드 |
| 손으로 만든 표 5종 — `방범CCTV_26개소.csv` · `ITS영상CCTV_26개소.csv` · `주정차허용_26개소.csv` · `보행약자축_26개소.csv` · `차종투입판정_26개소.csv` | 두 저장소 어디에도 생성 코드 없음 | 코드 자체 |
| `3단표.md` · `이름은있는데쓸수없는속성.md` · `도달률매트릭스_진입곤란26개소.md` | 손으로 쓴 문서 | — |
| 이태원 슬롯 시연 — `slot_itaewon_{demo,sop3,arm3_width}.py` (→ `derived/slot_itaewon_{arm1,arm2,arm3}*`) | 비공개 코어 모듈 | `DFLOW_008R9R4_CORE/` |
| 이태원 도로경계 내접폭 — `itaewon_medial_width.py` (→ `deliverables/이태원_도로경계내접폭.csv`) | **API 접근 차단으로 미산출 — 0건이 아니라 조회 실패.** 2026-09-18 실행에서 `api.vworld.kr` 로의 CONNECT 가 이 실행 환경의 이그레스 정책에서 403 으로 거부됨 | 브이월드 API 접근(키 + 등록 도메인 + 차단 없는 망) |
| 도표 5(e5) | 비공개 GeoJSON·HTML | `data/vworld/e5/` · `docs/vworld/e5/` |
| 원시 캐시 **재수급**(`fetch`/`sigg`) | 오늘 일부러 돌리지 않음 — 브이월드 현행 데이터와 09-15~16 캐시가 같다는 보장 없음 | — |
| 표준노드링크 · SAM 가중치 · 정사영상 타일 | 저장소에 없음(용량·이용조건). 이 PC 에서는 비공개 저장소 파일을 연결해 썼음 | `MOCT_LINK.shp` 276 MB + `.dbf` 336 MB · `sam_vit_b_01ec64.pth` 358 MB · 타일 422장 |
| `PREREG.md` 의 커밋 시각 10건 | 비공개 저장소 커밋 — 공개 저장소 git 객체에 없음(`git cat-file -e` 10/10 실패) | 검증 가능한 것은 `docs/prereg/` 본문의 blob SHA-1 뿐 |

---

## 5. 제3자 재현 시나리오 (README 첫머리에 요약)

**① 설치 없이** — `python scripts/verify_deliverables.py`(통과 38) · `python scripts/verify_numbers.py docs/서식2_본문_2026-09-18.txt .`(PASS) · `python scripts/slot_time_stage1.py`(바이트 일치) · `git hash-object docs/prereg/*`(10/10) · `python scripts/slot_itaewon_width.py`(표준 라이브러리 + 커밋된 원시 캐시 `raw/road_bt_national_cells.jsonl.gz` 만 — 네트워크·모델·비공개 코어 없음 → `deliverables/이태원_도로구간폭_판정.csv` 164구간 · BLOCKED 30 · CONDITIONAL_C 37 · 폭축_통과후보 97 재생성). 확인되는 것은 「산출물 안에서 수치가 서로 맞는가」까지다.

**② `pip install -r requirements.txt` 뒤(키·인터넷 없이)** — 커밋된 원시 캐시(`raw/*.jsonl.gz` 3종 · `raw/firewater/*.csv.gz` · 작은 원천)와 마스크 캐시(`derived/sam_vehicle_cache/` 48개 · 1.1 MB)로 `road_bt_national agg` · `road_bt_crosstab_export` · `sensitivity` · `sensitivity_boundary` · `site_table_26` · `water_slot_buildings` · `ai_a_classifier run` · `daejeon_cctv_match` · `ngii_five_way` · `transit_inflow(_volume)` · `audit_claims` 를 재실행할 수 있다. 오늘 새 venv(핀 그대로)에서 3개를 골라 돌려 동일함을 확인했다. 전국 78.2 % · 152,944 는 이 단계에서 나온다.

**③ 원천을 직접 받은 뒤** — 브이월드 키(+등록 도메인) · 표준노드링크(612 MB) · SAM 가중치(358 MB) · torch CPU 를 갖추면 나머지 — 내접폭·경사·연계 테이블·레이어 커버리지·포함률·위계 대조·회전 기하 전수·**SAM 사슬 처음부터** — 를 돌릴 수 있다. 이 PC 에서 SAM 사슬은 100 분 걸렸고 결과·마스크 캐시가 바이트 단위로 같았다. 정사영상 타일은 저장소에 없으므로 키로 자동 수급되며, **브이월드 타일이 촬영 판을 바꾸면 SAM 결과가 달라질 수 있다**(오늘은 09-15 캐시 타일로 돌렸다 — 이 점은 제3자가 재현할 때 같은 타일이라는 보장이 없다).

---

## 6. 판정

**이 저장소는 제3자가 검증할 수 있는가 — 「부분적으로, 그러나 어제보다 훨씬 넓게」.**
설치 없이 검산 38건과 시간 축, 패키지 설치 뒤 전국 집계·민감도·개소 집계·AI-A·교차표(오늘 새 venv 에서 동일 확인), 원천을 갖추면 SAM 사슬(3,838→709→351→71)까지 — 오늘 이 PC 에서 마스크 캐시를 지우고 처음부터 돌려 커밋본과 바이트 단위로 같았고 마스크 캐시 48개까지 같았다 — 를 재실행해 대조할 수 있다.
그러나 ① 판정 기준의 **사전 확정 시각**은 공개 저장소로는 검증되지 않고(본문 blob SHA-1 만 대조 가능), ② `deliverables/` 41개 중 손으로 만든 표 5종과 문서 3종은 코드로 재현되지 않으며, ③ `주장_출처대조.csv` 커밋본과 `조판편차_실측.json` 은 저장소에 없는 제출본 `.hwpx` 에 묶여 있고, ④ `AI-B`·`AI보완` 2종은 오늘 재실행하지 못했으며, ⑤ 원시 캐시 재수급과 정사영상 타일은 브이월드 현행 데이터가 09-15 캐시와 같다는 보장이 없어 「같은 입력」을 제3자가 확보한다는 보장이 없다.
따라서 「제3자가 저장소만으로 핵심 주장을 검증할 수 있다」는 문장은 **마스크 캐시·원시 캐시가 커밋돼 있다는 조건 아래 집계 단계까지는 참**이고, **SAM 단계는 가중치와 타일을 갖춘 사람에게만 참**이며, 사전 확정 시각과 손으로 만든 표는 여전히 작성자의 기록을 믿어야 하는 부분이다.
