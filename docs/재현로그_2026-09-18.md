# 재현 로그 — 2026-09-18

## 머리말

**실행 환경**

- 컨테이너 Linux · Python 3.11.15 (CPU 2코어 · RAM 7 GB)
- 설치 패키지: numpy 2.4.4 · pandas 3.0.2 · shapely 2.1.2 · pyproj 3.7.2 · pyogrio 0.13.0 · geopandas 1.1.4 · scipy 1.17.1 · scikit-learn 1.8.0 · pillow 12.2.0 · matplotlib 3.10.9 · requests 2.33.1
- **torch 미설치** · **segment_anything 미설치** · opencv(cv2) 는 import 가능
- 실행 일시: 2026-09-18 KST (18:03~18:08)
- 네트워크: api.vworld.kr · data.go.kr 도달 불가. `VWORLD_APIKEY` 미설정(빈 문자열).

**있었던 것 / 없었던 것**

있었던 것 — 저장소 전체 작업본, `data/vworld/national/derived/` 84개 산출물, 원시 셀 파일 3종
(`road_bt_national_cells.jsonl` 73 MB · `sprd_national_lt_cells.jsonl` 75 MB · `daejeon_sprd_geom.jsonl` 16 MB), `data/vworld/원천/` 14개 파일.
없었던 것 — 표준노드링크 `data/vworld/national/raw/NODELINKDATA_20260914/MOCT_LINK.shp`(디렉터리 자체가 없음), SAM 가중치
`data/vworld/models/sam_vit_b_01ec64.pth`, 브이월드 API 키·네트워크, 그리고 `audit_claims.py` 가 근거 파일로 읽는
`docs/vworld/데이터수급_실사.md` · `docs/vworld/학술문헌_조사.md` · `docs/vworld/기술조사_임무배정.md`(셋 다 공개 저장소에 없음).

대조 기준: 실행 전에 `deliverables/` 와 `data/vworld/national/derived/` 를 통째로 복사해 두고, 실행 뒤 파일별로
`diff <(tr -d '\r' < 재생성본) <(tr -d '\r' < 커밋본)` 으로 비교했다(커밋본 CSV 는 CRLF).

**스크립트 수정: 없다.** 14개 항목 어느 것도 코드 수정 없이 실행했다. `sam_vehicle_width.py` 의 `segment_anything` import 는
이미 `main()` 안(179행)에 있어 지연 import 로 바꿀 필요가 없었다.

---

## 표

| 스크립트 | 명령 | 종료코드 | 소요(초) | 산출 파일 | 커밋본과 동일? | 비고 |
|---|---|---|---|---|---|---|
| verify_deliverables.py | `python3 scripts/verify_deliverables.py` | 0 | 0.03 | 없음(검증만) | — | `통과 38 · 실패 0` |
| slot_time_stage1.py | `python3 scripts/slot_time_stage1.py` | 0 | 0.03 | deliverables/슬롯시간_26개소.csv | 동일 | 37조각 중 35조각만 행 생성(L 또는 W 결측 2조각 skip) |
| sensitivity.py | `python3 scripts/sensitivity.py` | 0 | 0.93 | deliverables/민감도_버퍼폭.csv · deliverables/민감도_규격선험.csv · derived/sensitivity.json | 3개 모두 동일 | `sam_vehicle_width` import 성공(torch 불필요) |
| turn_geometry_census.py | `python3 scripts/turn_geometry_census.py` | 1 | 0.41 | 없음 | — | MOCT_LINK.shp 없음. 하위명령 없음(`main()` 단일) |
| road_bt_national.py | `python3 scripts/road_bt_national.py agg` | 0 | 3.51 | derived/road_bt_national.json | **다름(1줄)** | 하위명령은 `fetch`·`agg` 둘뿐 |
| national_inclusion.py | `python3 scripts/national_inclusion.py sigg` | 1 | 0.45 | 없음 | — | MOCT_LINK.shp 없음 |
| national_inclusion.py | `python3 scripts/national_inclusion.py agg` | 1 | 3.51 | 없음 | — | MOCT_LINK.shp 없음(원시 jsonl 은 다 읽은 뒤 실패) |
| hierarchy_compare.py | `python3 scripts/hierarchy_compare.py` | 1 | 2.91 | 없음 | — | MOCT_LINK.shp 없음 |
| hierarchy_compare_v2.py | `python3 scripts/hierarchy_compare_v2.py agg` | 1 | 3.03 | 없음 | — | MOCT_LINK.shp 없음. 하위명령 `fetch`·`sido`·`agg` |
| inclusion_rate.py | `python3 scripts/inclusion_rate.py` | 1 | 1.37 | 없음 | — | MOCT_LINK.shp 없음 |
| verify_numbers.py | `python3 scripts/verify_numbers.py docs/서식2_본문_2026-09-18.txt .` | 0 | 0.04 | 없음(검증만) | — | `PASS`. 인자 수정 불필요 |
| audit_claims.py | `python3 scripts/audit_claims.py docs/서식2_본문_2026-09-18.txt .` | 0 | 5.48 | deliverables/주장_출처대조.csv | **다름(216행 vs 190행)** | 인자 수정 불필요 |
| linkage_table.py | `python3 scripts/linkage_table.py` | 1 | 0.42 | 없음 | — | `VWORLD_APIKEY 없음 …` 한 줄, 트레이스백 없음 |
| vworld_api_probe.py | `python3 scripts/vworld_api_probe.py` | 1 | 0.05 | 없음 | — | 같은 메시지, 트레이스백 없음 |
| determinism_check.py | `python3 scripts/determinism_check.py` | 1 | 0.11 | 없음 | — | `ModuleNotFoundError: No module named 'torch'` (15행, 모듈 최상단) |
| sam_vehicle_width.py | `python3 scripts/sam_vehicle_width.py` | 1 | 0.36 | 없음 | — | `ModuleNotFoundError: No module named 'segment_anything'` (179행) |
| acquire_open.py | `python3 scripts/acquire_open.py __no_such_source__` | 0 | 0.05 | (테스트 로그 생성 후 삭제) | — | 인자 없이 돌리면 6개 원본 **전부 다운로드**한다 — 그래서 돌리지 않았다 |

---

## 재현된 것

- **verify_deliverables.py** — `통과 38 · 실패 0`. 재생성 뒤 다시 돌려도 `통과 38 · 실패 0`.
- **slot_time_stage1.py** — `deliverables/슬롯시간_26개소.csv` 가 커밋본과 **바이트 단위로 같다**(줄끝 정규화 후).
  출력: 긴급+보행 동시통행 불가 22/35 · 긴급+일반 불가 29/35 · 대기 최소 9.1 s · 중앙 46.2 s · 최대 135.2 s.
- **sensitivity.py** — `민감도_버퍼폭.csv` · `민감도_규격선험.csv` · `derived/sensitivity.json` 셋 다 커밋본과 같다.
  torch 없이 돈다. 버퍼 0.8~1.2배 흔들 때 판정 불변율 최소 86.5 %(1.2배·5조각 변동) 재현.
- **road_bt_national.py agg** — 전국 875,892구간 · 잎칸 3,474 · 실패칸 0 · 시도 16개 집계가 **수치 전부 일치**.
  다른 것은 `rule_doc` 문자열 한 줄뿐(아래 절 참조).
- **verify_numbers.py** — `PASS`. 진실값 `{sam_raw_masks 3838, spec_pass 709, corridor_in 351, confirmed 71, outside_removed 638, outside_rate 90.0, n_pieces 37}`
  가 본문·json summary·CSV 합계 행에서 모두 일치.
- **linkage_table.py · vworld_api_probe.py** — 키 없음을 트레이스백 없이 한 줄로 알리고 종료한다(종료코드 1, `sys.exit(문자열)`).
  메시지: `VWORLD_APIKEY 없음 — 이 스크립트는 브이월드 API 조회 단계다. 원 실행 산출: …`

## 입력이 없어 못 돌린 것(무엇이 없는지 정확히)

없는 파일은 하나다: **`data/vworld/national/raw/NODELINKDATA_20260914/MOCT_LINK.shp`**
(표준노드링크 2026-09-14판 · shp 276 MB + dbf 336 MB · 사용자 PC 에만 있음). 상위 디렉터리
`NODELINKDATA_20260914/` 자체가 컨테이너에 없다. 다섯 스크립트가 여기서 멈춘다.

| 스크립트 | 멈춘 줄 | 에러 |
|---|---|---|
| turn_geometry_census.py | 102 | `pyogrio.errors.DataSourceError: …/NODELINKDATA_20260914/MOCT_LINK.shp: No such file or directory` |
| national_inclusion.py `sigg` | 40 | 같음 |
| national_inclusion.py `agg` | 87 | 같음 |
| hierarchy_compare.py | 98 | 같음 |
| hierarchy_compare_v2.py `agg` | 154 | 같음 |
| inclusion_rate.py | 147 | 같음(`bbox=(215000, 395000, 255000, 435000)` 대전창) |

**막힌 원인은 이 파일 하나뿐이다.** 네트워크도 키도 아니다. 근거:
`hierarchy_compare.py` 는 실패 전에 `road_bt_national_cells.jsonl` 875,892구간을 2.9초 동안 전부 파싱했고(네트워크 호출 없음),
`hierarchy_compare_v2.py agg` · `national_inclusion.py agg` 도 원시 jsonl 을 다 읽은 뒤 `pyogrio.read_dataframe` 한 줄에서 죽었다.
세 스크립트 모두 `KEY = os.environ.get("VWORLD_APIKEY", "")` 를 갖고 있지만 그 경로를 타지 않는다.

또 하나: **SAM 가중치 `data/vworld/models/sam_vit_b_01ec64.pth`** 와 정사영상 타일도 없다. 다만
`sam_vehicle_width.py` 는 가중치를 읽기 전에 `segment_anything` import 에서 먼저 죽으므로, 이번 실행에서 확인된 1차 차단은 패키지다.

**acquire_open.py 는 입력이 아니라 네트워크가 필요해서 돌리지 않았다.** `main(sys.argv[1:])` → `ids = argv or list(SOURCES)` 이므로
인자 없이 실행하면 `nodelink`(표준노드링크 전국 1파일) 포함 6개 원본을 **실제로 내려받는다**. `--help` 류 옵션은 없다.
대신 존재하지 않는 원본 id 로 인자 처리만 확인했다 — 3회 재시도 후 `{"status": "ERROR", "error": "KeyError: '__no_such_source__'"}`,
종료코드 0. 확인용으로 생긴 `data/vworld/national/acquire_log.json` 은 삭제했다. 실다운로드는 시도하지 않았다.

## 패키지가 없어 못 돌린 것

- **determinism_check.py** — 15행 모듈 최상단 `import torch`:
  ```
  Traceback (most recent call last):
    File "/home/claude/repo/scripts/determinism_check.py", line 15, in <module>
      import torch
  ModuleNotFoundError: No module named 'torch'
  ```
  기존 산출 `data/vworld/national/derived/determinism_check.json` 은 그대로 있다. 내용 요약:
  변정7길 한 조각을 캐시와 재실행으로 비교해 `n_masks 93 · n_spec 15 · n_corr 21 · n_conf 5 · lat_max 2.21` 이 완전히 같았고,
  `행 단위 완전 일치: true` · `집계 일치: true` · `소요초 127` · `torch 2.14.0+cpu` 로 기록돼 있다(이번 실행에서 재현한 값이 아니다).

- **sam_vehicle_width.py** — 179행:
  ```
  Traceback (most recent call last):
    File "/home/claude/repo/scripts/sam_vehicle_width.py", line 409, in <module>
      sys.exit(main())
    File "/home/claude/repo/scripts/sam_vehicle_width.py", line 179, in main
      from segment_anything import SamAutomaticMaskGenerator, sam_model_registry
  ModuleNotFoundError: No module named 'segment_anything'
  ```
  179행까지는 갔다 — `load_roads()`, `ortho_readings.json`, `도로경계내접폭_26개소.csv` 읽기는 성공했다는 뜻이다.
  아무것도 설치하지 않았다.

## 커밋본과 달라진 산출물

재생성한 6개 파일 중 **2개가 다르다**.

### 1. `data/vworld/national/derived/road_bt_national.json` — 1줄

```
2c2
<  "rule_doc": "docs/판정기준.md §10",
---
>  "rule_doc": "docs/vworld/회전기하_판정기준.md §10",
```

숫자는 전부 같다(전국 875,892구간 · eq_3_0_rate 0.3019 · 시도 16개 값 전부 일치). 다른 것은 자기가 따르는 판정기준 문서
경로 문자열 하나뿐이다. 커밋본은 판정기준 문서가 `docs/vworld/회전기하_판정기준.md` 이던 시절에 만들어졌고, 지금 스크립트는
`docs/판정기준.md` 를 가리킨다. 즉 **산출물이 스크립트보다 오래됐다** — 문서를 옮기고 스크립트를 고친 뒤 이 json 을 다시 뽑지 않았다.

### 2. `deliverables/주장_출처대조.csv` — 190행 → 216행

| 출처 유형 | 커밋본 | 재생성본 |
|---|---|---|
| 저장소 산출 | 154 | 158 |
| 1차 원문 | 29 | 26 |
| 2차 인용 | 7 | 13 |
| **미확인** | **0** | **19** |
| 합계 | 190 | 216 |

`(본문 구절, 추출)` 이 같은 공통 행은 152개다. 원인은 둘이다.

**(가) 입력 본문이 다르다.** 커밋본은 제출본 `.hwpx` 에서 뽑은 본문으로, 재생성본은 `docs/서식2_본문_2026-09-18.txt`
(.hwp 바이너리에서 문단 텍스트를 추출한 것)로 만들었다. 문단 최대 번호가 503 → 538 로 늘었고, 그래서
`37조각`(5회) · `100 m`(2회) · `243대` · `1.59 m` 같은 추출이 새로 잡히고, 반대로 `ISO 19157-1:2023`(2회) · `[문헌L]` ·
`[문헌O·P]` · `505시간` · `57만 장` · `9.9 m` 등은 사라졌다. `.hwpx` 는 `.gitignore` 에 걸려 저장소에 없으므로
이 차이는 공개 저장소만으로는 없앨 수 없다.

**(나) 근거 파일 3개가 저장소에 없다.** `audit_claims.py` 의 `EVIDENCE_GLOBS` 는
`docs/vworld/데이터수급_실사.md` · `docs/vworld/학술문헌_조사.md` · `docs/vworld/기술조사_임무배정.md` 를 읽는데
셋 다 저장소에 없다(`git ls-files docs/` 결과는 `docs/판정기준.md` 하나뿐). 그래서 공통 152행 중 **15행이
`저장소 산출` → `미확인`** 으로 내려앉았다:

```
78.2 %   저장소 산출 -> 미확인      31.6 %    저장소 산출 -> 미확인
97.6 %   저장소 산출 -> 미확인      18.5 %p   저장소 산출 -> 미확인
11,125건 저장소 산출 -> 미확인      7,948건   저장소 산출 -> 미확인 (2회)
71.4 %   저장소 산출 -> 미확인      2,763건   저장소 산출 -> 미확인
2,893 m  저장소 산출 -> 미확인      29.7 %    저장소 산출 -> 미확인 (2회)
8.1 %    저장소 산출 -> 미확인      6.00 m    저장소 산출 -> 미확인
9,974개 · 8,207개 · 82.28 %        저장소 산출 -> 미확인
```

덧붙여 `audit_claims.py` 는 세 번째 인자로 사람 판정 CSV(`주장_출처대조_사람판정.csv`)를 받아 `미확인` 행을 덮어쓰게 돼 있는데,
그 파일도 저장소에 없다(`EXCLUDE` 목록에만 이름이 있다). 커밋본에 `미확인` 이 0행인 것은 그 파일이 있는 환경에서 돌렸기 때문으로 보인다.

**정리: 이 산출물은 공개 저장소만으로는 재현되지 않는다.** 스크립트 탓이 아니라, 스크립트가 읽는 근거 파일 4개
(`.hwpx` 본문 · `docs/vworld/` 3종 · 사람판정 CSV)가 저장소에 없기 때문이다.

### 나머지 4개 파일

`슬롯시간_26개소.csv` · `민감도_버퍼폭.csv` · `민감도_규격선험.csv` · `sensitivity.json` — **모두 동일**.

---

## 부기 — 실행 중 눈에 띈 것(값은 고치지 않았다)

- `road_bt_national.json` : 전국 875,892구간 중 `road_bt` 가 정확히 `3.0` 인 구간이 264,398개(30.19 %),
  `0` 인 구간이 4,311개다. 파일 자체가 `"unit_assumption": "m 가정 — 명세 미확인"` 을 달고 있다.
  30 %가 한 값에 몰린 것은 기본값 채움을 의심할 근거다.
- `road_bt_national.json` 의 시도 이름 표기가 섞여 있다 — 13개는 `서울`·`부산` 같은 짧은 이름인데
  `전남광주통합특별시(12)` · `강원특별자치도(51)` · `전북특별자치도(52)` 세 개만 코드가 괄호로 붙어 있다.
  스크립트 안 `SIDO` 딕셔너리의 값 표기가 통일돼 있지 않다.
- `슬롯시간_26개소.csv` 는 37조각이 아니라 35조각이다. `도로경계내접폭_26개소.csv` 의 `면안축선(m)` 또는
  `통행가능폭_주차차감_37조각.csv` 의 `최종채택(m)` 이 비어 있는 2조각을 `continue` 로 건너뛴다.
  파일 안에도 스크립트 출력에도 「몇 조각을 왜 뺐는지」는 남지 않는다.
- `determinism_check.json` 은 `torch 2.14.0+cpu` 환경에서 만들어진 기록이다. 이 컨테이너에는 torch 가 없으므로
  결정성 재확인은 하지 못했다 — 기록을 읽었을 뿐이다.

---

---

## 추가 실행 — 브이월드 레이어 커버리지 (2026-09-18 19:17 KST, 사용자 PC · Windows · Python 3.13)

`scripts/vworld_layer_probe_26.py` · 16개 레이어 × 37조각 · 반경 100 m · **조회 실패 0건**.
개발키는 발급 시 등록한 도메인이 같이 가야 한다(`domain=`). 이것을 빼고 돌린 1차 시도는 592건 전부
`ServiceExceptionReport` 를 받았고, 그 결과를 **0건이 아니라 「조회 실패」로 기록**했다. 도메인을 붙인 2차 시도가 아래다.

| 레이어 | 한글명 | 피처 있는 조각 | 피처 합계 |
|---|---|---|---|
| lt_c_a3drivewaysection | 차도구간 | **0 / 37** | 0 |
| lt_c_a5parkinglot | 주차면 | **0 / 37** | 0 |
| lt_l_c5heightbarrier | 높이장애물 | **0 / 37** | 0 |
| lt_c_c4speedbump | 과속방지턱 | **0 / 37** | 0 |
| lt_c_a4subsidiarysection | 보도구간 | 5 / 37 | 29 |
| lt_l_a2link | 주행경로링크 | 6 / 37 | 37 |
| lt_p_a1node | 주행경로노드 | 3 / 37 | 10 |
| lt_c_b3surfacemark | 노면표시 | 4 / 37 | 17 |
| lt_l_b2surfacelinemark | 노면선표시 | 6 / 37 | 94 |
| lt_c_b1safetysign | 안전표지 | 4 / 37 | 17 |
| lt_c_usfsffb | 소방서관할구역 | 37 / 37 | 42 |
| lt_p_utiscctv | 교통CCTV | 34 / 37 | 140 |
| lt_c_up201 | 재해위험지구 | 4 / 37 | 4 |
| lt_c_up401 | 급경사재해예방지역 | 0 / 37 | 0 |
| lt_c_uq125 | 방재지구 | 0 / 37 | 0 |
| lt_c_uq128 | 취락지구 | 0 / 37 | 0 |

- **정밀도로지도 계열의 차도구간·주차면·높이장애물·과속방지턱은 37조각 전부 0건이다.** 미조회가 아니라
  조회에 성공하고 0건이다. 폭·높이 축을 국가 정밀공간정보가 이 골목들에 대해 갖고 있지 않다는 뜻이며,
  위성영상과 AI 로 폭을 대신 재려 한 이유가 여기에 있다.
- 같은 계열이라도 보도구간·주행경로링크·노면표시 계열은 5~6조각에서 값이 나온다. 「계열 전체가 없다」로 쓰지 않는다.
- 교통CCTV 34/37(140건)은 이 저장소의 `방범CCTV_26개소.csv`(다른 원천)와 별개다. 두 CCTV 를 섞지 않는다.
  영상 제공 여부는 이 조회로 알 수 없으므로 관측 가능성 행은 바꾸지 않는다.
- 산출: `deliverables/브이월드_레이어_커버리지_26개소.csv` · `data/vworld/national/derived/vworld_layer_probe_26.json`
  (조각 단위로 `count` 와 `err` 을 따로 남겨, 0건과 미조회가 사후에도 구분된다)

이 로그는 실제 실행 결과다. 돌리지 않은 것은 돌리지 않았다고 적었다.
