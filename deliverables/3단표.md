# 3단 표 — 지금 되는 것 / 한 칸만 열리면 되는 것 / 안 되는 것

작성 2026-09-16 · 기준 `회전기하_판정기준.md` §27 (b543dde) · 「지금 되는 것」은 실측 결과로만 채움 · 대상 대전 진입곤란 26개소

## ① 지금 되는 것 (실측)

| 무엇 | 결과 | 근거 |
|---|---|---|
| 지정 골목 선형 찾기 | 26/26 개소가 도로명주소도로(브이월드 `lt_l_sprd`)에 있음 · 정사영상 대표 11조각 선형 일치(사람 판독, C) | §8 · §9 |
| 표준노드링크와 이어진 곳 가려내기 | 연계 테이블 — 26개소 전부 U1(같은 도로명 링크 없음) · 매칭은 다른 7개소의 「로」 18행 | §14 |
| 판정 불가를 판정 불가로 표시 | 폭 판정 가능 0 — 모든 조각이 UNKNOWN_FAIL_CLOSED 로 남음(AI 보완 후에도 26/26) | §25 · §26 |
| 결측이 결측으로 보이지 않는 속성 찾기 | road_bt 전국 결측 0 % · 3.0 단일값 30.2 % / 표준노드링크 이름 「-」 69,247링크 4.43 % | §12 · §13 정정 |
| 기하(G) 값 산출(AI 아님) | 건물 외곽 간격 값이 나오는 조각 23/37 · 전부 C 등급 · 판독과 차이 6/8 | §25 |

## ② 한 칸만 열리면 되는 것

| 열릴 원천 | 채워지는 칸 | 바뀌는 판정 |
|---|---|---|
| 도로명주소 전자지도 **실폭도로**(juso 신청 처리 중) | 폭 L1 을 A 등급 원천으로 | 폭 L3 × → 판정 가능 여부를 A 등급 값으로 다시 판정 · 기하(G)·SAM 결과의 A 등급 대조군 |
| 도로명주소 전자지도 **출입구**(같은 신청) | 진입점 | 진입점 후보를 건물 좌표가 아니라 출입구로 |
| 진입곤란 지정 시 **링크 ID 병기**(제안) | 선형 L2 | 연계 테이블 U1 → 매칭 |
| 지자체 **CCTV 설치 좌표**(표준데이터 접근) | 관측 L1 26개소 ? | 골목 관측 원천 유무 확인 |

## ③ 안 되는 것 (지금 원천·기준이 없음)

| 무엇 | 이유 |
|---|---|
| 정사영상 기반 노면 폭 판정(오픈 모델) | SAM ViT-B 가 검증셋에서 판독과 차이 7/8 — 모델 기반 실패(016 F) · 촬영 해상도 원본 여부 미증명 |
| 경사 판정 | DEM 미수급 · 사다리차 아웃트리거 허용 경사 기준값 미확보 |
| 높이·노면 판정 | 원천 조회했으나 확인 못 함(?) |
| 골목 공개 영상 관측 | 공개 영상 제공 대상이 고속도로·국도 한정(ITS 문구) |
| 통과 확정 | C 등급(기하·모델)은 통과를 열지 못함 — A·B 등급 폭 원천이 없음 |
