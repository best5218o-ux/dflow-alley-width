# 작업지시서 2 — 「돌지 않던 스크립트」를 전부 실제로 돌린다

작성 2026-09-18 21:15 KST · 수행자: 로컬 Claude Code · 작업 위치 `C:\Users\대전\dflow-alley-width`
선행 지시서: `작업지시서_저장소_재현가능화.md` (P0 완료 · P1 진행 중 · 커밋 `df22249` 미푸시)

---

## 0. 이 작업의 목적과, 절대 하면 안 되는 것

목적은 **「환경이 없어서 못 돌렸다」를 없애는 것**이다. 지금 못 도는 이유는 코드 결함이 아니라
키·대용량 원천·torch·메모리가 그 자리에 없었기 때문인데, **그 넷이 전부 이 PC 안에 있다.**

### ⛔ 절대선 — 어기면 이 작업 전체가 무효다

1. **「성공」을 만들려고 값을 맞추지 마라.** 재실행 결과가 커밋본과 다르면 그것은 **발견**이지 고칠 대상이 아니다.
   숫자를 맞추려고 입력·임계·코드를 바꾸는 순간 이 저장소의 존재 이유가 사라진다.
2. **제출본이 인용한 수치를 바꾸지 마라.** 오늘 제출하는 서식2 본문은 이미 확정됐고
   `deliverables/` 의 값을 그대로 인용한다. 재실행이 다른 값을 내면 **커밋본을 덮어쓰지 말고**
   차이를 보고서에 적어라. 덮어쓸지는 사람이 판단한다.
3. **미조회를 「없다」로 쓰지 마라.** 조회 실패는 0건이 아니다.
4. **돌리지 않은 것을 「확인함」으로 적지 마라.**
5. 비공개 저장소(`C:\Users\대전\dflow-disaster-orchestration`)는 **읽기 전용**. 제출본 폴더
   (`C:\Users\대전\Downloads\브이월드_공모전\`)는 **접근 금지**. `git push` 금지.
6. **키를 코드·로그·커밋·산출물에 남기지 마라.** `os.environ` 경유만.

### 실행 전 공통 준비 (매번)
```powershell
cd C:\Users\대전\dflow-alley-width
$env:VWORLD_APIKEY = [Environment]::GetEnvironmentVariable('VWORLD_APIKEY','User').Trim()
$env:VWORLD_DOMAIN = 'localhost:8765'      # 개발키 등록 도메인. 이것이 없으면 전건 ServiceException
```
**중요 — 어디서 돌리는가**: 브이월드 API 는 **Windows 의 python 에서만** 닿는다.
Cowork/WSL 쪽 리눅스 셸에서는 프록시가 403 을 돌려준다. 네트워크가 필요한 것은 전부 PowerShell 에서 돌려라.

### 덮어쓰기 방지 규약 (모든 실행에 적용)
```powershell
# 실행 전
robocopy deliverables _run\before\deliverables /E /NFL /NDL /NJH /NJS
robocopy data\vworld\national\derived _run\before\derived /E /NFL /NDL /NJH /NJS
```
실행 후 `fc` 또는 python `difflib` 로 대조하고, **차이가 있으면 원본을 복원한 뒤 보고**하라.
`_run\` 은 `.gitignore` 에 추가해 커밋되지 않게 한다.

---

## 1. 지금 상태 (2026-09-18 21:11 실측)

| 분류 | 개수 | 상태 |
|---|---|---|
| 실제로 돌려 커밋본과 일치 확인 | 15 | ✅ verify_deliverables(38/0)·slot_time_stage1·sensitivity·sensitivity_boundary·transit_inflow(_volume)·road_bt_national agg·national_inclusion agg·hierarchy_compare·hierarchy_compare_v2 agg·verify_numbers·audit_claims·site_table_26·ngii_five_way·road_bt_crosstab_export |
| 진행 중 | 1 | ⏳ `sam_vehicle_width.py` — 캐시 31/37 |
| 브이월드 키+망 필요 | 약 24 | ❌ 아직 |
| 대용량 원천 필요 | 8 | ❌ 아직 |
| torch/SAM 필요 | 4 | ❌ (SAM 뒤) |

---

## 2. 할 일

### A. SAM 사슬 완주 — **최우선. 이것이 이 저장소의 핵심 주장이다**
`sam_vehicle_width.py` 가 끝나면:
1. 새 `data/vworld/national/derived/sam_vehicle_width.json` 과 **커밋본을 비교**하라.
   확인할 값: 원 마스크 **3,838** · 규격 통과 **709** · 버퍼 안 **351** · 차량 후보 **71** ·
   버퍼 밖 제외율 **90.0 %** · 회랑필터 합계 행(3838/709/351/71/638/90.0).
2. **일치하면** → 재현로그에 「SAM 사슬 재현 확인」으로 적고, 산출물은 그대로 둔다.
3. **다르면** → 커밋본을 **절대 덮어쓰지 말고**, 어느 값이 얼마나 다른지·원인 가설(타일 캐시 차이,
   패키지 버전, 난수)을 적어 보고하라. 제출본이 이 숫자들을 인용하고 있으므로 사람 판단이 필요하다.
4. `python scripts\verify_deliverables.py` 를 다시 돌려 **38통과·0실패**가 유지되는지 확인.
5. **`data/vworld/national/derived/sam_vehicle_cache/` 를 커밋하라.** 지금 커밋에서 빠져 있는데,
   이것이 `sensitivity.py`·`sensitivity_boundary.py` 의 **유일한 입력**이다. 빠지면 제3자는
   민감도 3종을 재실행할 수 없고, 제출본 15쪽의 「고정 커밋 — 산출물·민감도 결과」가 절반만 참이 된다.
   (약 488 KB. `.gitignore` 에 걸리지 않는지 `git check-ignore -v` 로 확인할 것)

### B. torch 계열 3종
SAM 이 끝난 뒤, 같은 venv 에서:
- `determinism_check.py` — 변정7길 재실행. 커밋본 값(마스크 93 · n_spec 15 · n_corr 21 · n_conf 5 ·
  lat_max 2.21 · 행 단위 완전 일치 true)과 대조. 제출본 15쪽이 「마스크 93개·차량 후보 5대가 행 단위로
  같았다」·「CPU 127초」를 인용한다.
- `sam_validation.py` · `model_availability.py` — 실행하고 결과를 기록.

### C. 브이월드 키 필요 계열 (약 24개) — **가장 큰 덩어리**
키는 있고 도메인도 확인됐다(`localhost:8765`). **PowerShell 에서** 하나씩 돌려라.

⚠️ **재수급 주의**: `road_bt_national.py fetch` · `hierarchy_compare_v2.py fetch` ·
`national_inclusion.py sigg` 같은 **fetch/수급 단계는 원시 캐시(`raw/*.jsonl.gz`)를 새로 채운다.**
브이월드 데이터가 그사이 바뀌었으면 전국 집계 수치가 달라지고, 제출본이 인용한 78.2 %·152,944 가
흔들린다. → **fetch 계열은 `_run\fetch\` 같은 별도 경로로 받아 비교만 하고, 저장소의 캐시를 덮어쓰지 마라.**
스크립트가 경로를 상수로 갖고 있으면 환경변수나 인자로 우회할 방법을 찾되, **집계 로직은 건드리지 마라.**

돌릴 대상(예시 — `scripts\` 전수 확인해 목록을 직접 만들 것):
`vworld_api_probe` · `vworld_layer_probe_26` · `inclusion_rate` · `linkage_table` · `slope_26` ·
`ngii_medial_width` · `ngii_width_probe` · `ngii_bound_cover` · `ngii_obstacle_diag` ·
`water_slot_buildings` · `fire_water_coverage` · `daejeon_cctv_check` · `daejeon_cctv_match` ·
`transit_inflow` 계열 · `road_bt_daejeon` · `alley_census` · `site_fire_ward` 등

각각에 대해: **명령 · 종료코드 · 소요 · 산출물이 커밋본과 같은가 · 다르면 어디가**.

### D. 대용량 원천 필요 계열 (8개)
표준노드링크는 같은 PC 의 비공개 저장소에 있다:
`C:\Users\대전\dflow-disaster-orchestration\data\vworld\national\raw\NODELINKDATA_20260914\`
**복사하지 말고**(276+336 MB, 커밋 대상 아님) 심볼릭 링크 또는 junction 으로 연결해 돌려라:
```powershell
# 관리자 PowerShell 에서
New-Item -ItemType Junction -Path "C:\Users\대전\dflow-alley-width\data\vworld\national\raw\NODELINKDATA_20260914" `
         -Target "C:\Users\대전\dflow-disaster-orchestration\data\vworld\national\raw\NODELINKDATA_20260914"
```
`.gitignore` 가 이미 `NODELINKDATA_*/` 를 제외하므로 커밋되지 않는다(확인할 것).

#### D-1. `turn_geometry_census.py` — 메모리 부족을 실제로 푸는 법 (이 건은 반드시 성공시켜라)

**죽는 자리를 특정했다.** 로그가 `moves 2230134 2227815` 까지 찍고 죽었으므로,
그 다음 줄인 **`L["R_link"] = min_radius(geoms, L["len"].values)`** 에서 터진다.

원인은 `min_radius()` 가 **전국 링크 156만 개를 한 번에** 10 m 간격으로 표본화하기 때문이다:
```python
offs = np.concatenate([...])                       # 수천만 개
pts  = shapely.line_interpolate_point(geoms[gi], offs)   # ← GEOS Point 객체 수천만 개 (수 GB)
d    = pd.DataFrame({"i":…, "o":…, "x":…, "y":…})        # 4열 × 수천만 행 (수 GB)
g.x.shift(-1) / shift(-2)                                # 사본이 또 3벌
```
링크 하나의 최소 회전반지름은 **다른 링크와 독립**이다. 따라서 **링크를 묶음으로 잘라 돌려도
결과가 비트 단위로 같다.** 이것은 알고리즘 변경이 아니라 순수한 메모리 최적화다.

**적용할 패치 — `min_radius` 를 청크 루프로 감싼다 (계산식은 한 글자도 바꾸지 마라)**
```python
def min_radius(geoms, lengths, chunk=20000):
    """2026-09-18: 전국 156만 링크를 한 번에 표본화하면 메모리가 터진다.
    링크별 최소 회전반지름은 서로 독립이므로 묶음으로 나눠 계산한다 — 결과는 동일하다."""
    n = len(geoms)
    out = np.full(n, np.nan)
    for s in range(0, n, chunk):
        e = min(s + chunk, n)
        sub = _min_radius_block(geoms[s:e], lengths[s:e])   # 기존 본문을 그대로 옮긴 함수
        out[s:e] = sub
    return out
```
`_min_radius_block` 은 **지금 `min_radius` 본문을 그대로 복사**해서 만든다(인덱스가 블록 로컬이
되는 것 외에 바꿀 것이 없다). 끝나면 **검증**하라 — 작은 표본(예: 앞 5만 링크)에 대해
청크 적용 전/후 결과가 `np.allclose(..., equal_nan=True)` 로 같은지 확인하고 로그에 적어라.

**같이 적용할 것**
1. `end_vectors(geoms)` 도 전국 좌표를 한 번에 펼친다. 위와 같은 방식으로 청크화하라(독립 계산이다).
2. 큰 중간 객체는 쓰고 나서 즉시 버려라 — `del pts, d, xy; import gc; gc.collect()`.
3. 이동 테이블 `M` 의 문자열 열(`a_LINK_ID`·`ROAD_NAME` 등)은 `astype("category")` 로 바꾸면
   수백 MB가 준다. **값은 변하지 않는다.**
4. `chunk` 는 20,000 부터 시작해 메모리가 남으면 50,000 으로 올려라. 반대로 또 죽으면 5,000 으로 낮춰라.
5. 진행 상황을 `print(f"radius {s}/{n}", flush=True)` 로 찍어, 어디까지 갔는지 남겨라.

**보조 수단 (패치로 부족할 때만)**
- 가상 메모리 확대: 시스템 속성 → 고급 → 성능 설정 → 고급 → 가상 메모리 → 사용자 지정 크기
  (예: 처음 32768 MB / 최대 65536 MB). SSD 라면 느려도 완주한다.
- 64비트 python 인지 확인: `python -c "import sys,struct; print(struct.calcsize('P')*8)"` → 64 여야 한다.
- 다른 큰 프로그램(한글·브라우저·GitHub Desktop)을 닫고 돌려라.
- 오래 걸리면 백그라운드로 돌리고(`Start-Process`) 다른 작업을 진행하라.

**성공 판정** — 산출 5종이 나오고 커밋본과 대조된다:
`turn_census.json` · `access37_reverse.json` · `access37_links.csv` ·
`daejeon_indistinct.csv` · `sharp_moves_targets.csv`
특히 로그의 **`moves 2230134 2227815`** 두 숫자가 재현되는지 먼저 확인하라(여기까지는 이미 돌던 구간이다).

**⛔ 하지 말 것** — 표본 링크 수를 줄이거나 `STEP` 을 10 m 에서 키워서 「성공」시키지 마라.
그건 다른 계산이고, 결과 수치가 달라진다. 메모리만 줄이고 계산은 그대로 두는 것이 조건이다.
정말 안 되면 **「필요 메모리 추정 N GB, 이 PC 로는 못 돌림」**이라고 측정치와 함께 적어라.
- `acquire_open.py` — 실제 다운로드를 수행한다. **무인자 실행 금지.** 인자 처리와 URL 유효성만 확인하고,
  필요하면 원천 1종만 `_run\` 에 받아 접근 가능함을 보이라.
- `missing_rates_bridge_tunnel.py` — 교량표준데이터가 미커밋이다. 비공개 저장소 raw 에서 링크해 돌려라.

### E. README 에 적힌 명령 전수 검증
README 의 모든 명령을 **그대로 복사해 실행**해 보고, 실패하는 것은 README 를 고쳐라.
이미 알려진 것: `factsheet.py` 는 `--pdf-json` 인자가 필수인데 README 에 없다.

---

## 3. 산출물

### 3-1. `docs/재현로그_2026-09-18.md` 에 이어 적기 (기존 표 형식 유지)
스크립트 | 명령 | 종료코드 | 소요 | 산출물이 커밋본과 같은가 | 비고

### 3-2. `docs/재현성_감사_2026-09-18.md` (신규)
1. **61개 스크립트 전수 분류표** — 이름 | 무엇을 만드는가 | 필요한 것 | 실행함? | 결과
2. **실행 성공 / 실패 각각의 정확한 사유** (「환경이 없어서」가 아니라 무엇이 없어서인지 파일명·용량까지)
3. **커밋본과 달라진 산출물** — 어느 파일 어느 값이 얼마나, 원인 가설, 덮어쓰지 않은 이유
4. **제3자 재현 시나리오 3단계** (①설치 없이 ②pip 후 ③원천 수급 후 각각 어디까지) — README 맨 앞에 요약
5. 마지막 한 문장: **이 저장소는 제3자가 검증할 수 있는가.** 좋게 쓰지 말고 사실대로.

### 3-3. 커밋
- `sam_vehicle_cache/` 포함 여부를 **반드시 확인**하고 커밋
- `_run\`·`NODELINKDATA_*`·대용량 원천이 실수로 들어가지 않았는지 `git status` 와
  `git diff --cached --stat | tail -1` 로 확인 (GitHub 는 100 MB 초과 파일을 거부한다)
- 커밋 메시지는 **무엇이 되고 무엇이 안 되는지**를 사실대로. 「재현 가능해졌다」처럼 검증 안 된 주장 금지
- **푸시하지 않는다.** 사용자가 GitHub Desktop 에서 한다.

---

## 4. 보고 규칙

각 단계가 끝날 때마다 한 줄로 보고하라. 특히 다음은 **즉시** 보고하라:
- SAM 사슬 결과가 커밋본과 **다를 때** (제출본이 인용한 숫자다)
- 재수급 결과가 커밋본과 **다를 때**
- 100 MB 를 넘는 파일이 커밋에 들어가려 할 때
- 키가 어딘가에 기록될 위험을 발견했을 때

**오늘 23:59 가 접수 마감이다.** 제출본은 이미 확정되어 이 작업과 독립이지만,
**커밋 `df22249` 는 제출본 본문이 가리키는 커밋이므로 반드시 푸시 가능한 상태로 유지**해야 한다.
그 커밋을 되돌리거나 히스토리를 고쳐 쓰지 마라.
