# 작업지시서 — 공개 저장소를 「제3자가 내려받아 재현할 수 있는」 상태로 만들기

작성 2026-09-18 20:00 KST · 수행자: 로컬 Claude Code · 작업 위치 `C:\Users\대전\dflow-alley-width`

---

## 0. 배경 (먼저 읽을 것)

`dflow-alley-width` 는 제8회 공간정보 활용·아이디어 경진대회(GEO-AI 혁신 아이디어 부문) 제출본의
**재현 저장소**다. 제출본 본문이 "이 저장소에서 확인할 수 있다"고 적고 있으므로, **저장소가 실제로
재현되지 않으면 제출본의 주장이 거짓이 된다.** 이 작업의 목적은 그 간극을 없애는 것이다.

오늘 외부 감사에서 나온 판정:
> "부분적으로만 검증 가능하다 — `verify_deliverables.py` 38개 검산과 `slot_time_stage1.py` 까지는
> 누구나 확인할 수 있고 비트 단위로 일치했으나, 원시 캐시·MOCT_LINK·SAM 가중치·API 키가 배포본에
> 빠져 있어 핵심 주장인 SAM 차량탐지 사슬과 전국 집계는 제3자가 스스로 재계산할 수 없다."

### 이 프로젝트의 절대 규칙 (어기면 작업 전체가 무효다)
1. **미조회를 「없다」로 쓰지 않는다.** 조회에 실패한 것은 0건이 아니라 「조회 실패」로 남긴다.
2. **산출된 값을 손으로 고치지 않는다.** 값이 이상하면 사유를 문서에 적을 뿐, 숫자를 바꾸지 않는다.
3. **못 한 것은 못 했다고 적는다.** 「해결됨」은 실제로 실행해서 확인한 것에만 쓴다.
4. **인증키·개인정보는 저장소에 남기지 않는다.** 키는 `os.environ` 으로만 읽고, 로그·산출물에 마스킹한다.
5. **제출본(`.hwp`/`.pdf`)은 건드리지 않는다.** `C:\Users\대전\Downloads\브이월드_공모전\` 는 읽지도 쓰지도 말 것.
6. **`git push` 는 하지 않는다.** 커밋까지만 하고 푸시는 사용자가 GitHub Desktop 에서 한다.

---

## 1. 지금 상태

- 공개 저장소: `C:\Users\대전\dflow-alley-width` (git, 원격 `best5218o-ux/dflow-alley-width`)
- 비공개 작업 저장소: `C:\Users\대전\dflow-disaster-orchestration` — **읽기만 한다. 절대 수정하지 않는다.**
  - `docs\vworld\scripts\` — 스크립트 61개 (공개본에는 16개뿐)
  - `docs\vworld\deliverables\` — 산출물 (공개본에 없는 AI-* 계열 등이 있다)
  - `docs\vworld\*.md` — 조사 문서 (데이터수급_실사 · 학술문헌_조사 · 기술조사_임무배정 등)
  - `data\vworld\national\raw\NODELINKDATA_20260914\` — 표준노드링크 SHP (276MB+336MB)
  - `data\vworld\models\sam_vit_b_01ec64.pth` — SAM 가중치 358MB
  - `data\vworld\ortho_cache\` — 브이월드 정사영상 타일 캐시
- 오늘 이미 끝난 것:
  - `data\vworld\national\derived\` 84종, `raw\*.jsonl.gz` 3종, `data\vworld\원천\` 5종을 공개본에 넣었다
  - `.gitignore` 를 다시 썼다 (예전엔 `data/` 전체를 무시해서 위 파일들이 커밋조차 되지 않았다)
  - `scripts\_rawio.py` 추가 — 평문 `.jsonl` 이 없으면 `.jsonl.gz` 를 연다
  - `scripts\transit_inflow.py` 확장 · `scripts\transit_inflow_volume.py` 신규 · `scripts\vworld_layer_probe_26.py` 신규
  - `scripts\slot_time_stage1.py` 수정 — 입력 결측 2조각을 건너뛰지 않고 사유와 함께 37행으로 남긴다
  - `docs\재현로그_2026-09-18.md` 작성

---

## 2. 할 일 (우선순위 순)

### P0. 비공개 → 공개 자료 이관
비공개 저장소에서 공개 저장소로 **복사**한다(이동 아님. 원본은 그대로 둔다).

1. **스크립트** — `dflow-disaster-orchestration\docs\vworld\scripts\*.py` 중 공개본 `scripts\` 에 없는 것 전부
2. **산출물** — `...\docs\vworld\deliverables\` 중 공개본 `deliverables\` 에 없는 것 전부
   (특히 `AI-C_도로경계내접폭_5자대조.csv`, `AI-A_모델유사골목_상위.csv`, `AI-B_SAM검증_4자대조.csv`,
   `AI보완_커버리지표.csv`, `26개소_산출표.md`, `대전_단속CCTV_골목급대조.csv`)
3. **조사 문서** — `...\docs\vworld\데이터수급_실사.md`, `학술문헌_조사.md`, `기술조사_임무배정.md`
   → 공개본 `docs\vworld\` 에 (`audit_claims.py` 의 `EVIDENCE_GLOBS` 가 이 경로를 읽는다)

**복사 전 반드시 확인할 것 — 공개해서는 안 되는 것이 섞여 있는지:**
- 인증키·토큰 (`grep -riE "key\s*=\s*['\"][A-Za-z0-9-]{20,}|apikey|confmkey"`)
- 개인정보 (이름·주민번호·전화번호·이메일·주소). 단 제출자 본인의 공개용 연락처는 이미 제출본에만 있고
  저장소엔 없어야 한다 — 발견되면 **복사하지 말고 목록만 보고**할 것
- 제3자 저작물 원문(PDF 등), 재배포가 금지된 원천 데이터
발견 시 **그 파일은 복사하지 말고 보고서에 이유와 함께 적는다.**

### P1. 경로 수정 후 전부 실행
복사한 스크립트는 비공개 저장소 배치(`docs/vworld/deliverables/`, `docs/vworld/scripts/`)를 전제로 쓰였다.
공개 저장소는 `deliverables/`, `scripts/` 가 루트에 있다. **각 스크립트의 `ROOT`/`DELIV`/`DER` 상수와
`sys.path` 삽입을 공개 배치에 맞게 고친다.** 로직·수치·판정 규칙은 건드리지 않는다.

그 다음 **하나씩 실제로 실행**한다. 실행 전에 덮어쓸 산출물을 `_audit\before\` 로 복사해 두고,
실행 후 `diff` 로 커밋본과 같은지 확인한다. **다르면 왜 다른지 밝히고, 커밋본을 덮어쓸지 말지 판단해 보고한다**
(원칙: 재실행 결과가 맞고 커밋본이 낡았으면 교체한다. 입력이 달라 생긴 차이면 커밋본을 유지하고 사유를 적는다).

실행 대상과 알려진 상태:
| 스크립트 | 지금 상태 | 필요한 것 |
|---|---|---|
| `verify_deliverables.py` | 통과 38 · 실패 0 | 없음 (무의존) |
| `slot_time_stage1.py` | 37행 재현 확인 | 없음 |
| `sensitivity.py` | 재현 확인 | 없음 |
| `transit_inflow.py` / `_volume.py` | 재현 확인 | 없음 |
| `road_bt_national.py agg` | 재현 확인 | `raw/*.jsonl.gz` (있음) |
| `national_inclusion.py agg` | 재현 확인 | MOCT_LINK.shp |
| `hierarchy_compare.py` / `_v2.py agg` | 재현 확인 | MOCT_LINK.shp |
| `inclusion_rate.py` | **미재현** — 프록시 403 | VWORLD_APIKEY + 외부망 |
| `turn_geometry_census.py` | **미재현** — OOM | 메모리 |
| `sam_vehicle_width.py` | **미재현** | torch + SAM 가중치 + 타일 캐시 |
| `determinism_check.py` | **미재현** | torch |
| `audit_claims.py` | **불일치** 216행 vs 커밋 190행 | 위 P0-3 문서 3종 |

브이월드 조회가 필요한 것은 환경변수로 돌린다(키를 코드·로그에 남기지 말 것):
```powershell
$env:VWORLD_APIKEY = [Environment]::GetEnvironmentVariable('VWORLD_APIKEY','User')
$env:VWORLD_DOMAIN = 'localhost:8765'
```
`turn_geometry_census.py` 는 메모리 부족으로 죽는다. **알고리즘을 바꾸지 말고**, 청크 처리나
스왑 확대로 돌려 보고, 그래도 안 되면 「이 PC 메모리로는 못 돌림」이라고 적는다.

SAM 사슬(`sam_vehicle_width.py`)은 이 저장소의 **핵심 주장**(원 마스크 3,838 → 규격 통과 709 →
버퍼 안 351 → 차량 후보 71)을 만든 코드다. torch(CPU)·SAM 가중치·`data\vworld\ortho_cache\` 가
비공개 저장소에 다 있으므로, **가능하면 실제로 돌려서 커밋된 `sam_vehicle_width.json` 과 대조하라.**
이것이 재현되면 저장소 신뢰도가 가장 크게 올라간다. 오래 걸리면(37조각 약 400초) 백그라운드로 돌리고
다른 작업을 진행한다.

### P2. 문서 바로잡기
1. **`deliverables\산출물_명세.md`** — 지금 28개 중 2개만 설명한다. **전 파일을 설명하도록 다시 쓴다.**
   파일마다: 무엇인가 / 행·열 / 생성 스크립트 / 기준 문서 절 / **읽을 때 조심할 것**(빈 칸의 의미,
   분모, 미산출 표기). 오늘 감사에서 나온 아래 사항을 반드시 반영한다:
   - `슬롯순서_26개소.csv` · `용수도달성_26개소.csv` 의 「개소」 열이 26행 전부 비어 있다 → 왜 비었는지
     스크립트를 읽어 밝히고 적는다. (값을 채우지 말 것)
   - `방범CCTV_26개소.csv` 는 53행이고 연번 22·30 이 결번이며 26개소/37조각 어느 체계도 아니다 → 구조를 밝혀 적는다
   - `경사_26개소.csv` · `보행약자축_26개소.csv` 의 경사 열은 사실상 전 행 미산출이다 → 파일명·열명이
     산출된 것처럼 읽히는 점을 적는다
   - `도로명위계_전국대조.csv` 에 스크립트가 만들지 않는 수기 주석 행 1줄이 있다
   - `전국대조군_길번길포함률.csv` 의 합산·평균 행이 열 수가 짧다(래그드 CSV)
   - `차량탐지_사람대조_11조각.csv` 숫자 열에 `null`·`대조 불가`·서술문이 섞여 있다
2. **`README.md`** — `python scripts/verify_numbers.py` 가 인자 없이 `IndexError` 로 죽는다.
   실제 사용법(`verify_numbers.py <제출본 본문.txt 또는 .hwpx> <저장소 루트>`)으로 고친다.
   README 가 적은 모든 명령을 **실제로 한 번씩 실행해** 그대로 도는지 확인한다.
3. **`docs\판정기준.md`** — 참조하는 산출물 파일이 전부 존재하는지 대조한다.
   (`deliverables/AI-C_도로경계내접폭_5자대조.csv` 가 지금 없다 → P0 로 들어오면 해결)
4. **`PREREG.md`** — 열거된 사전 확정 커밋 10건이 **이 저장소의 git 객체에 없다**(`git cat-file -e` 10/10 실패).
   비공개 저장소의 커밋이라 제3자가 조회할 수 없다. 값을 지어내지 말고,
   **「제3자 검증 불가」임을 PREREG 첫머리에 분명히 적는다.** 가능하면 각 커밋 시점의
   `판정기준.md` 본문을 `docs/prereg/<단축해시>_판정기준.md` 로 함께 넣어, blob SHA-1 을
   제3자가 직접 계산해 대조할 수 있게 한다(`git hash-object` 로 검증 명령도 README 에 적을 것).
5. **`docs\재현로그_2026-09-18.md`** — 오늘 새로 돌린 결과를 이어 적는다. 형식은 기존 표를 따른다.

### P3. 커밋
- `.gitignore` 가 의도대로 동작하는지 확인: `git check-ignore -v` 로 `data/vworld/national/derived/*`,
  `raw/*.jsonl.gz`, `data/vworld/원천/*` 가 **무시되지 않는지**, NODELINKDATA·models·ortho_cache·*.pth 는
  **무시되는지** 둘 다 확인한다.
- `git add -A` 후 `git status` 로 **용량 큰 파일이 실수로 들어가지 않았는지** 확인
  (`git diff --cached --stat | tail -1`, 100MB 넘는 파일은 GitHub 이 거부한다).
- 커밋 메시지는 사실만 적는다. 「재현 가능해졌다」처럼 검증하지 않은 주장을 쓰지 말고,
  무엇을 넣었고 무엇이 여전히 안 되는지 적는다.
- **푸시하지 않는다.**

---

## 3. 보고서

작업이 끝나면 `docs\재현성_감사_2026-09-18.md` 에 다음을 적는다.

1. **이관 목록** — 복사한 파일, 복사하지 않은 파일과 그 이유(개인정보·키·용량·저작권)
2. **실행 표** — 스크립트 | 명령 | 종료코드 | 소요 | 커밋본과 동일? | 비고
3. **커밋본을 교체한 산출물**과 그 이유
4. **여전히 재현되지 않는 것**과 정확한 이유(무엇이 없어서인지 파일명·용량·이유까지)
5. **제3자 재현 시나리오** — 저장소만 내려받은 사람이 ①설치 없이 ②pip install 후 ③원천을 직접 받은 뒤,
   각각 어디까지 확인할 수 있는지 단계별로. 이것을 README 맨 앞에 요약해 넣는다.
6. 마지막 한 문장: **이 저장소는 제3자가 검증할 수 있는가**에 대한 판정과 근거.
   좋게 쓰지 말고 사실대로 쓸 것.

---

## 4. 하지 말 것 (재확인)

- 비공개 저장소 수정 · 제출본 폴더(`Downloads\브이월드_공모전\`) 접근 · `git push`
- 산출된 수치를 손으로 고치기 · 없는 값을 채우기 · 「미조회」를 「0건」으로 쓰기
- 키·개인정보를 저장소·로그·커밋 메시지에 남기기
- 실행하지 않은 것을 「확인함」으로 적기
